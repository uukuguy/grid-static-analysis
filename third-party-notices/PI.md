# Pi runtime notice

Grid Agent acquires `@earendil-works/pi-coding-agent` from `https://github.com/earendil-works/pi.git` at commit `2b3fda9921b5590f285165287bd442a25817f17b`.

The pinned package version is `0.80.6`, distributed under the MIT license.

Runtime acquisition is independent of research checkouts. The Grid Agent runtime manager installs only the pinned Pi source into its managed state directory and does not search or reuse ambient development copies.
